import { Heading, Image, Text, FlatList, Item, Input, Stack, Button, ScrollView, Box, FormControl, View, Center } from "native-base";
import { Header } from "../components";

const Form = () => {
  return (
    <>
      <Header title={"Izumi"} />
      <Center flex={1}>
        <Heading>Form</Heading>
      </Center>
    </>
  );
};

export default Form;